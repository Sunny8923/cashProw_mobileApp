import 'package:flutter/material.dart';

class AppColors {
  // ✅ Brand
  static const Color primary = Color(0xFF8B0000);

  // ✅ Shades (for gradient / hover / dark)R
  static const Color primaryDark = Color(0xFF4A0000);
  static const Color primaryLight = Color(0xFFFF7A18);

  // ✅ Backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFFFF6F0);
  static const Color tint = Color(0xFFFFF1E6); // optional

  // ✅ Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);

  // ✅ Borders
  static const Color border = Color(0xFFE5E7EB);

  // ✅ Status
  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFF59E0B);
}
